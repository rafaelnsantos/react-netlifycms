import React from 'react';
import { IdentityWidgetProps, useIdentityWidget } from './useIdentityWidget';

export const IdentityWidget = (props: IdentityWidgetProps) => {
  useIdentityWidget(props);

  return <></>;
};
